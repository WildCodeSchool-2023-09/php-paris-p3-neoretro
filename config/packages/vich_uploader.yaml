vich_uploader:
    db_driver: orm

    metadata:
        type: attribute

    mappings:
       poster_file:
            uri_prefix: /images/game-posters
            upload_destination: "%upload_directory%"
            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer
