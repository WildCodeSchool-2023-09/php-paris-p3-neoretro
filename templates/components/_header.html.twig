<header>
    <img id="main-logo" src="{{ asset('build/images/icons/icon.png') }}" alt="NeoRetro Logo">

    {% if pageTitle == 'NeoRetro' %}
        <h1 id="page-title" class="michroma">NeoRetro</h1>
    {% else %}
        <h1 id="site-name" class="michroma">NeoRetro</h1>
        <h2 id="page-title" class="michroma">{{pageTitle}}</h2>
    {% endif %}

    {% if admin is defined and true %}
        <h2 id="admin" class="michroma">Admin</h2>
    {% endif %}
</header>