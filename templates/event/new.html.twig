{% extends 'base.html.twig' %}

{% block content %}

<div class="card full-size" id="event-form">
    <div class="content">
        <h2>Event details</h2>

        {# <a href="{{ path('event_index') }}">Back to list</a> #}

        <div class="container">

            {% include 'event/_form.html.twig' %}

        </div>
    </div>
</div>

{% endblock %}
