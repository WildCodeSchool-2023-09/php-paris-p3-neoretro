{% extends 'base.html.twig' %}

{% block content %}

<div id="add-event" class="card full-size">
    <div class="content">
        <h2>New event</h2>

        {# <a href="{{ path('event_index') }}">Back to list</a> #}

        <div class="container">

            {% include 'event/_form.html.twig' %}

        </div>
    </div>
</div>

{% endblock %}
