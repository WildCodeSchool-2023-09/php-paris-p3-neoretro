{% extends 'base.html.twig' %}

{% block content %}

<div class="card full-size" id="game-form">
    <div class="content">
        <h2>{{ gameForm.title.vars.value }}</h2>

        <div class="container">
            {{ include('game/_form.html.twig') }}
        </div>
    </div>
</div>  

{% endblock %}
