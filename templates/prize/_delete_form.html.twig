<form method="post" action="{{ path('prize_delete', {'slug': prize.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prize.slug) }}">
    <button class="btn">Delete</button>
</form>
